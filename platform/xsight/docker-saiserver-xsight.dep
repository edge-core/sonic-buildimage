DPATH       := $($(DOCKER_SAISERVER_XSIGHT)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) $(PLATFORM_PATH)/docker-saiserver-xsight.mk $(PLATFORM_PATH)/docker-saiserver-xsight.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_SAISERVER_XSIGHT)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_SAISERVER_XSIGHT)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SAISERVER_XSIGHT)_DEP_FILES   := $(DEP_FILES)

