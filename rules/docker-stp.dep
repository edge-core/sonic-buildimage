
DPATH       := $($(DOCKER_STP)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-stp.mk rules/docker-stp.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_STP)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_STP)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_STP)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_STP),$(DOCKER_STP_DBG)))
